services:
  postgres:
    image: postgres
    ports:
    - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: argon_auth
    volumes:
      - postgres:/data/postgres

volumes:
  postgres:
